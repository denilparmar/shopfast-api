name: Deploy ShopFast API

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    env:
      AWS_REGION: us-east-1
      AWS_DEFAULT_REGION: us-east-1
      USER_POOL_ID: ${{ secrets.USER_POOL_ID }}
      USER_POOL_CLIENT_ID: ${{ secrets.USER_POOL_CLIENT_ID }}

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout code
        uses: actions/checkout@v3

      # 2️⃣ Setup Node.js (LTS)
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'  # LTS version recommended

      # 3️⃣ Configure AWS credentials
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      # 4️⃣ Install Serverless Framework (core-only, no dashboard login)
      - name: Install Serverless Framework
        run: npm install -g serverless@4.30.0 --no-dashboard

      # 5️⃣ Install project dependencies (ignore peer dependency issues)
      - name: Install Node dependencies
        run: npm install --legacy-peer-deps

      # 6️⃣ Deploy Serverless API (uses AWS credentials, no dashboard login)
      - name: Deploy Serverless API
        run: serverless deploy --stage prod --no-telemetry
